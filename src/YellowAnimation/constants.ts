import { Transition } from "framer-motion";

export const STROKE_WIDTH = 4;

export const TRANSITION: Transition = {
  repeat: Infinity,
  ease: "linear",
  repeatType: "reverse",
  repeatDelay: 0,
};
